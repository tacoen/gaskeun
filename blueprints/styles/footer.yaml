name: Footer
type: base

form:
  fields:

    background:
      type: input.colorpicker
      label: Background
      default: "#e0e0f0"

    text-color:
      type: input.colorpicker
      label: Text color

