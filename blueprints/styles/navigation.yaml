name: Navigation / Offcanvas
type: sections

form:
  fields:

    background:
      type: input.colorpicker
      label: Background
      default: "#556699"

    active-color:
      type: input.colorpicker
      label: Active Color

    hover-color:
      type: input.colorpicker
      label: Hover Color
